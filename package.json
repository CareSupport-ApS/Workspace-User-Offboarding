{
  "name": "workspace-user-offboarding",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:client && npm run build:server && npm run build:manifest",
    "build:client": "vite build",
    "build:server": "node scripts/build-gas-server.mjs",
    "build:manifest": "node scripts/copy-manifest.mjs",
    "dev": "vite"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.0.1"
  }
}
